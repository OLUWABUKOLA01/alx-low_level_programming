#include "lists.h"

/**
 * list_len - function to list length of string
 *
 * @h: Const
 *
 * Return: Length of string
 */

size_t list_len(const list_t *h)
{
	unsigned int a = 0;

	while (h == 0)
	
		return (a);
}
